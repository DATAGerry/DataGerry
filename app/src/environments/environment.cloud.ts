export const environment = {
    production: false,
    cloudMode: true,
    preCloudMode: false,
    featurePreviewMode: false,
    protocol: 'http',
    apiUrl: '', // API URL
    apiPort: 4000 // Typically HTTPS
};