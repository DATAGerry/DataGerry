export const environment = {
    production: false,
    cloudMode: true,
    preCloudMode: true,
    featurePreviewMode: true,
    protocol: 'http',
    apiUrl: '127.0.1.1', // API URL
    apiPort: 4000 // Typically HTTPS
};